cottonCanvas = "Picasso"
woodCanvas = "Monet"
scrapCanvas = "empty"

# You can only copy paintings onto another canvas
# using the following commands:

# cottonCanvas = woodCanvas
# cottonCanvas = scrapCanvas
# woodCanvas = cottonCanvas
# woodCanvas = scrapCanvas
# scrapCanvas = woodCanvas
# scrapCanvas = cottonCanvas

print(cottonCanvas)
print(woodCanvas)
